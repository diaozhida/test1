def classify(inX,dataSet,labels,k):
  '''k-近邻算法：inX分类的输入值，dataSet：输入的训练样本集，labels：标签，k最近邻居的数目'''
  pass

def createDataSet():
  group =np.array([1.0,1.1],[1.0,1.0],[0,0.1])
  labels=['A','A','B','B']
  return group,labels
